<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Update Baby Shower RSVP</title>

  <script src="https://cdn.tailwindcss.com"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

  <style>
    body { font-family: 'Roboto', sans-serif; }

    .animate-fadeIn {
      animation: fadeIn 1s ease forwards;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px);}
      to { opacity: 1; transform: translateY(0);}
    }
  </style>
</head>

<body class="bg-pink-50 text-gray-800">

<!-- Main Content -->
<div class="min-h-screen flex flex-col md:flex-row">

  <!-- Left Image -->
  <div class="relative md:w-1/2 w-full h-64 md:h-auto bg-cover bg-center"
       style="background-image: url('/assets/images/radhakrishna.jpg');">
    <div class="absolute inset-0 bg-black/30 flex items-center justify-center text-center">
      <h1 class="text-white text-4xl font-bold">Update Your RSVP 🎀</h1>
    </div>
  </div>

  <!-- Right Section -->
  <div class="md:w-1/2 w-full flex flex-col justify-center items-center px-6 py-10"
       style="background-image: url('/assets/images/bg.jpg');">

    <div class="max-w-md w-full bg-white shadow-2xl rounded-3xl p-8 animate-fadeIn">

      <h2 class="text-2xl font-semibold mb-2 text-center text-pink-600">
        Update Your Baby Shower RSVP
      </h2>

      <p class="text-center text-lg mb-4 text-gray-700">
        We’re excited to celebrate with you 👶
      </p>

      <!-- Event Info -->
      <div class="text-sm text-center mb-6 space-y-2">
        <p><strong>Date:</strong> March 8, 2026</p>
        <p><strong>Time:</strong> 9:30 AM onwards</p>
        <p><strong>Address:</strong> H.M.S. Banquet Hall, Ajax, ON</p>

        <a href="https://www.google.com/maps/place/H.M.S.+Banquet+Hall"
           target="_blank"
           class="inline-flex items-center gap-2 text-blue-600 hover:underline">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5"
               fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M3 9.75L12 3l9 6.75M4.5 10.5v9.75h5.25V15h4.5v5.25H19.5V10.5"/>
          </svg>
          View Location
        </a>
      </div>

      <!-- Update RSVP Form -->
      <form id="rsvpForm"
            method="post"
            action="/api/rsvp/updatersvpRecord"
            class="space-y-4">

        <input type="text"
               name="name"
               value="{{name}}"
               readonly
               class="w-full px-4 py-2 border rounded-xl bg-gray-100">

        <input type="email"
               name="email"
               value="{{email}}"
               readonly
               class="w-full px-4 py-2 border rounded-xl bg-gray-100">

        <select name="attendance"
                class="w-full px-4 py-2 border rounded-xl">
          <option value="">Will you attend?</option>
          <option value="yes" {{#if attendance}}selected{{/if}}>
            Yes, I’ll attend
          </option>
          <option value="no" {{#if attendance}}selected{{/if}}>
            Sorry, I can’t make it
          </option>
        </select>

        <div class="flex gap-2">
          <input type="number"
                 name="adults"
                 min="0"
                 value="{{adults}}"
                 placeholder="Adults"
                 class="w-1/2 px-4 py-2 border rounded-xl">

          <input type="number"
                 name="kids"
                 min="0"
                 value="{{kids}}"
                 placeholder="Kids"
                 class="w-1/2 px-4 py-2 border rounded-xl">
        </div>

        <button type="submit"
                class="w-full bg-pink-600 text-white font-semibold py-2 rounded-xl hover:bg-pink-700 transition">
          Update RSVP
        </button>
      </form>

      <p id="confirmationMsg"
         class="mt-4 text-green-600 font-medium text-center"></p>

    </div>

    <!-- Footer -->
    <footer class="text-center text-sm text-gray-600 mt-6">
      <h3 class="bg-pink-500 text-white px-4 py-2 rounded-xl">
        Please Update Before February 15, 2026
      </h3>
    </footer>

  </div>
</div>

<script>
  const params = new URLSearchParams(window.location.search);
  const msg = params.get("msg");
  if (msg) {
    document.getElementById("confirmationMsg").innerText = msg;
  }
</script>

</body>
</html>
