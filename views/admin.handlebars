<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>RSVP Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center px-4">
  <div class="grid grid-cols-3 gap-4 text-center">
  <div class="bg-white p-4 rounded-xl shadow">
    <h3 class="text-lg font-semibold">Adults</h3>
    <p class="text-2xl font-bold text-blue-600">{{totalAdults}}</p>
  </div>

  <div class="bg-white p-4 rounded-xl shadow">
    <h3 class="text-lg font-semibold">Kids</h3>
    <p class="text-2xl font-bold text-pink-600">{{totalKids}}</p>
  </div>

  <div class="bg-white p-4 rounded-xl shadow">
    <h3 class="text-lg font-semibold">Total Guests</h3>
    <p class="text-2xl font-bold text-green-600">{{totalGuests}}</p>
  </div>
</div>

</body>
</html>
