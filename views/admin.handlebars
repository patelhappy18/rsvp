<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>RSVP Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center px-4">
  <div class="bg-white shadow-xl rounded-xl p-8 max-w-xl w-full">
    <h1 class="text-2xl font-bold mb-4 text-center">📋 RSVP Dashboard</h1>
    <p class="text-lg mb-6 text-center text-green-600 font-semibold">Total RSVPs: {{totalGuests}}</p>

    <h2 class="text-lg font-semibold mb-2">Latest RSVPs:</h2>
    <ul class="space-y-2">
      {{#each rsvps}}
        <li class="border-b pb-2">
          <strong>{{this.name}}</strong> ({{this.email}}) — Attending: {{this.attendance}}, Guests: {{this.guests}}
        </li>
      {{/each}}
    </ul>
  </div>
</body>
</html>
